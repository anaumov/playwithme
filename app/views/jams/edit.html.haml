= simple_form_for @jam, :html => {:class => "form-horizontal"} do |f|
  = f.error_notification
  .form-inputs
    = f.input :title
    = f.input :owner_id, as: :hidden

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'