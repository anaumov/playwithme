= simple_form_for @user, url: my_update_path, method: :put, :html => {:class => "form-horizontal"} do |f|
  = f.error_notification
  .form-inputs
    = f.input :full_name
    = f.input :website
    = f.input :public_email
    = f.input :location
    %p Загрузить аватар
    = f.file_field :avatar
    %br

  .form-actions
    = f.button :submit, :class => 'btn btn-primary'